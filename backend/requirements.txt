# ============================================================
# ITBIS Backend — Python Dependencies
# ============================================================

# ─── FastAPI & ASGI ─────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2

# ─── Configuration & Validation ─────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
email-validator==2.3.0

# ─── PostgreSQL / SQLAlchemy ─────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# ─── MongoDB ─────────────────────────────────────────────────
motor>=3.6,<4
pymongo>=4.9,<4.10

# ─── Elasticsearch ───────────────────────────────────────────
elasticsearch[async]==8.16.0

# ─── Redis ───────────────────────────────────────────────────
redis[hiredis]==5.2.1

# ─── Kafka ───────────────────────────────────────────────────
aiokafka==0.11.0

# ─── MinIO ───────────────────────────────────────────────────
minio==7.2.11

# ─── Authentication & Security ───────────────────────────────
python-jose[cryptography]==3.3.0
# passlib 1.7.4 + bcrypt 4.x are incompatible: bcrypt 4.0 removed the
# `__about__` attribute that passlib probes during its one-time
# version-detection step.  The probe falls back silently and hashing
# still works, but the warning pollutes every test run.  Pin bcrypt
# to the last 3.x release which still exposes `__about__`.
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.18

# ─── Logging ─────────────────────────────────────────────────
structlog==24.4.0
python-json-logger==2.0.7

# ─── Utilities ───────────────────────────────────────────────
python-dateutil==2.9.0
pytz==2024.2
orjson==3.10.12
tenacity==9.0.0

# ─── ML & Data (Phase 5 anomaly detection) ─────────────────
# The shipped model artifact was built with scikit-learn 1.6.1.
# Python 3.14 in our dev env does not have a pre-built wheel for 1.6.x
# (only 1.7.2+); we pin to 1.7.2 which is pickle-compatible enough to
# unpickle and run the artifact.  Production must use a Python version
# for which 1.6.1 wheels exist (3.11 / 3.12) and pin accordingly.
numpy>=1.26,<3
pandas>=2.2,<3
joblib>=1.4,<2
scikit-learn>=1.7,<1.8

# ─── Testing ─────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
factory-boy==3.3.1
faker==33.1.0
fakeredis==2.37.1
mongomock-motor==0.0.36
aiosqlite==0.22.1

# ─── Code Quality ────────────────────────────────────────────
ruff==0.8.4
mypy==1.13.0
